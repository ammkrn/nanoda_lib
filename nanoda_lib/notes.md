Constructors do not produce a `Step<A>` term, since application
of a constructor is considered axiomatic, and an expression's printed
representation in the parent step is a direct pointer to its application.